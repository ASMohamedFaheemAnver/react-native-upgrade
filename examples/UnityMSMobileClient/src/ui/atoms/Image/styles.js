import {CommonHeights, CommonWidths} from '@constants/numbers';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  defaultImage: {
    width: CommonWidths.cardImage,
    height: CommonHeights.cardImage,
  },
});
